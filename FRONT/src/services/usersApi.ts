import { adminHeaders } from './api'
const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:4000/api'
export async function getUsers(){const response=await fetch(`${API_URL}/users`,{headers:adminHeaders()});if(!response.ok)throw new Error('No se pudieron cargar los usuarios');return response.json()}
export async function createUser(payload:{username:string;password:string;role:'admin'|'editor'}){const response=await fetch(`${API_URL}/users`,{method:'POST',headers:{'content-type':'application/json',...adminHeaders()},body:JSON.stringify(payload)});const result=await response.json();if(!response.ok)throw new Error(result.error);return result}
export async function updateUser(id:string,payload:{active?:boolean;role?:'admin'|'editor';password?:string}){const response=await fetch(`${API_URL}/users/${id}`,{method:'PATCH',headers:{'content-type':'application/json',...adminHeaders()},body:JSON.stringify(payload)});const result=await response.json();if(!response.ok)throw new Error(result.error);return result}
export async function changeMyPassword(currentPassword:string,newPassword:string){const response=await fetch(`${API_URL}/me/password`,{method:'PATCH',headers:{'content-type':'application/json',...adminHeaders()},body:JSON.stringify({currentPassword,newPassword})});const result=await response.json();if(!response.ok)throw new Error(result.error);return result}
export async function downloadBackup(){const response=await fetch(`${API_URL}/backup`,{headers:adminHeaders()});if(!response.ok)throw new Error('No se pudo descargar la copia');const blob=await response.blob();const url=URL.createObjectURL(blob);const link=document.createElement('a');link.href=url;link.download=`gringo-shop-${new Date().toISOString().slice(0,10)}.sqlite`;link.click();URL.revokeObjectURL(url)}
