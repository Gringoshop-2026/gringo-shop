import { useState } from 'react'
import { changeMyPassword } from '../services/usersApi'
export default function PasswordManagement(){const [current,setCurrent]=useState('');const [next,setNext]=useState('');const [confirm,setConfirm]=useState('');const [message,setMessage]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();setMessage('');if(next!==confirm){setMessage('Las contraseñas nuevas no coinciden.');return}try{await changeMyPassword(current,next);sessionStorage.clear();setMessage('Contraseña cambiada. Vuelve a iniciar sesión.')}catch(e){setMessage(e instanceof Error?e.message:'No se pudo cambiar la contraseña')}};return <section className="mt-6 rounded-xl border bg-white p-6"><h2 className="font-display text-2xl font-semibold">Seguridad de tu cuenta</h2><p className="mt-1 text-sm text-[#68717a]">Cambia tu contraseña de acceso al panel.</p><form onSubmit={submit} className="mt-5 grid gap-3 md:max-w-xl"><input required type="password" value={current} onChange={e=>setCurrent(e.target.value)} placeholder="Contraseña actual" className="rounded-md border p-3"/><input required minLength={8} type="password" value={next} onChange={e=>setNext(e.target.value)} placeholder="Nueva contraseña (8+ caracteres)" className="rounded-md border p-3"/><input required minLength={8} type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Repite la nueva contraseña" className="rounded-md border p-3"/><button className="rounded-md bg-[#1c2b39] p-3 font-bold text-white">Cambiar contraseña</button>{message&&<p className="text-sm font-semibold text-[#a9553f]">{message}</p>}</form></section>}
